const lights = {
    night: "NIGHT_LIGHT",
    wake: "WAKE_LIGHT"
}

const sounds = {
    night: "NIGHT_SOUND",
    wake: "WAKE_SOUND"
}

const sleepTime = "SLEEP_TIME"
const relaxationTime = "RELAXATION_TIME"

export const events = {
    sleepTime,
    lights,
    sounds,
    relaxationTime
}

export const defaults = {
    sleepTime
}